# Vending Machine App( React, Sass, WebPack).

1. Please run this command in your terminal `npm install` in order to installed all needed modules from *package.json*<br>

2. For running project `npm run dev`<br>
